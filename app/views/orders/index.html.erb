<%= provide :title, t(".all_orders") %>
<div class="flex flex-col items-center justify-center mb-4">
  <%= render "filter" %>
</div>

<div data-controller="export" class="mb-4">
  <button id="export"
          data-action="export#export"
          class="w-36 my-4 px-4 py-2 text-white bg-blue-500 rounded-3xl hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 cursor-pointer">
    <%= t ".download" %>
  </button>
  <div class="export-status" data-export-target="status"></div>
</div>

<%= render "table" %>

<div class="mt-4">
  <%== pagy_nav @pagy %>
</div>
